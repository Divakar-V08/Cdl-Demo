pipeline {
    agent any

    stages {
        stage('Check Jenkinsfile Execution') {
            steps {
                echo 'Jenkinsfile is executing successfully!'
            }
        }
    }
}
